/*import 'package:flutter/material.dart';
import 'package:demo_app/role/widgets/widget_home_products.dart'; // Assuming this is needed for future use

class HomePage extends StatelessWidget {
  const HomePage({Key? key}) : super(key: key);

  @override
  Widcd get build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Home Page'),
      ),
      body: Container(
        child: ListView(
          children: [
            const Center(child: Text("Hello")),
            // You can add more widgets here, like the products widget
            // WidgetHomeProducts(), // Uncomment if needed
          ],
        ),
      ),
    );
  }
}
*/